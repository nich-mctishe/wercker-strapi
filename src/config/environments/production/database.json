{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "host": "${process.env.MONGO_TCP || '172.17.0.2'}",
        "port": "${process.env.MONGO_PORT || 27017}",
        "database": "api",
        "username": "${process.env.DB_USERNAME}",
        "password": "${process.env.DB_PASSWORD}"
      },
      "options": {}
    }
  }
}
