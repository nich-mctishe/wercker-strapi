{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "host": "${process.env.DOKKU_MONGO_API_PORT_27017_TCP_ADDR || '172.17.0.2'}",
        "port": "${process.env.DOKKU_MONGO_API_PORT_27017_TCP_PORT || 27017}",
        "database": "api",
        "username": "${process.env.DB_USERNAME}",
        "password": "${process.env.DB_PASSWORD}"
      },
      "options": {}
    }
  }
}
